int valor;

int value;

int soma(int x, int y);
int none();

begin
    write "Informe um valor:";
    read valor;

    while (valor > 0)
    begin
        write "\nvalor:";
        write valor;
        valor = (5 + 4);
        soma(valor,2);
    end
end


int soma(int x, int y)
begin
    x = (x+y);
end

int none()
begin
    write "Informe um valor:";
    read valor;
end